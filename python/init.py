"""Auto-loaded by OpenGothic at startup (if $CWD/python/init.py exists).

Everything executed here runs in the Marvin `py` REPL's persistent
global namespace — so anything you define or import here is available
the moment you hit F2 in-game.

Use this as a dotfile for your Gothic sessions: import your helpers,
set up aliases, preload utility modules.
"""

from tools import (
    where,
    living_npcs,
    noon,
    midnight,
    warp,
    full_heal,
    watch_hp,
)
